SYSTEM NOTIFICATION BACKEND 

RUN => node index.js

Notes:

it's using sqlite as database in memory, and it's generating all tables when it starts. same way it is inserting some dummy data
look at databaseconfig.js file

